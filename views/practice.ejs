<html>
    <head>
        <title>
            <%= title %>
        </title>
        <body>
            <h1>play ground</h1>
            <ul>
                <% for (let i=1,i<6,i++) { %>
                    <li>
                        <%= i %>
                    </li>
                    <% } %>
            </ul>
            <% if(true) { %>
                <h1>true</h1>

            <% } else{ %>
                <h1>false</h1>
            <% } %>
        </body>
    </head>
</html>